<% const photo = card.photo_url && /^https:\/\//i.test(card.photo_url) ? card.photo_url : ''; %>
<% if (photo) { %>
  <img src="<%= photo %>" alt="<%= card.name %>" class="profile-photo">
<% } else { %>
  <div class="default-avatar">
    <i class="fas fa-user"></i>
  </div>
<% } %>
